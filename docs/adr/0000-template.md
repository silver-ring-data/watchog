# NNNN. <결정 제목 — 예: 시계열 결합에 윈도우 기반 조인을 적용한다>

> 새 결정마다 이 파일을 `docs/adr/NNNN-짧은-제목.md` 로 복사한다 (NNNN 은 4자리 순번). 한 번 쓰면 고치지 않고, 뒤집을 땐 새 ADR 을 쓰고 이 문서 상태를 "대체됨"으로 바꾼다.
> 레포 `CLAUDE.md` "설계 결정" 표엔 한 줄 + 이 파일 링크만 둔다.

| 상태 | 날짜 |
|---|---|
| 제안 / **채택** / 보류 / 대체됨 (→ NNNN) / 폐기 | YYYY-MM-DD |

## 현상

<어떤 상황·문제가 있었나. 제약(시간·비용·환경) 포함.>

## 결정

<무엇을 하기로 했나. 구현 방식이 정해졌으면 한 줄로.>

## 이유

<왜 그렇게 했나. 다른 선택지를 버린 이유가 있으면 여기에.>

## 트레이드오프

<이 결정으로 감수하는 것. 다시 봐야 할 조건(예: 처리량이 X 를 넘으면)이 있으면 함께.>
